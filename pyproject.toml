[project]
name = "isaflow-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.4",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "customtkinter>=5.2",
    "matplotlib>=3.9",
]

[tool.uv]
package = false
